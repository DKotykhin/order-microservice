services:
  order-app:
    container_name: order-app
    image: order-microservice:latest
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - ./.env.docker
    ports:
      - 5005:5005
    networks:
      - coffeedoor-network

networks:
  coffeedoor-network:
      external: true